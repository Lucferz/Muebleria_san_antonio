# Ignora los archivos de la carpeta img
public/img/*

#Ignora la carpeta base de datos
Base de datos

#Ignora la carpeta pruebas
pruebas

#Ignora la carpeta tests
tests

#Ignora el archivo phpunit.xml y el cache
phpunit.xml
.phpunit.result.cache

#ignora la carpeta vendor de composer
vendor

#ignora los archivos json de composer
composer.json
composer.lock